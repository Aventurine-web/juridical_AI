# Test prompts

Prompts to demo the agent. Run with `adk run agent` (or `adk web`).
Fill these in at 16:00 with prompts that show off the real mission —
include at least one that triggers a tool call (e.g. `web_search`).

1. <!-- TODO: a simple happy-path prompt -->
2. <!-- TODO: a prompt that forces a tool call -->
3. <!-- TODO: a harder / multi-step prompt -->
4. <!-- TODO: an edge case (shows robustness) -->
5. <!-- TODO: the "wow" prompt for the demo recording -->
